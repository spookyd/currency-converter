import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    footnote: {
        color: '#FFFFFF',
        fontSize: 14,
        alignItems: 'center',
        textAlign: 'center',
    },
});

export default styles